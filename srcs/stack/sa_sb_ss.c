#include "stack.h"

/*
** swap - swap the first 2 elements at the top of stack a.
** Do nothing if there is only one or no elements).
*/

void swap(t_stack *stack)
{

}

void swap_two(t_stack *stack1, t_stack *stack2)
{
	if (stack1)
		swap(stack1);
	if (stack2)
		swap(stack2);
}